# 需求实现差距分析

> 对照 `require` 文件逐条检查。**我负责后端，前端是同学负责。**

---

## 后端已完成

| 模块 | 接口 | 状态 |
|------|------|------|
| AI | `/api/ai/generate-disclosure` 生成交底书 | ✅ |
| AI | `/api/ai/check-ai-rate` AI 率检测 | ✅ |
| AI | `/api/ai/generate-spec` 生成说明书 | ✅ |
| AI | `/api/ai/generate-claims` 生成权利要求书 | ✅ |
| AI | `/api/ai/review` 质检审核 | ✅ |
| AI | `/api/ai/check-terminology` 术语一致性 | ✅ |
| AI | `/api/ai/innovate` 创新思路 | ✅ |
| AI | `/api/ai/upload` 文件上传 | ✅ |
| AI | `/api/ai/generate-five-books` 生成摘要+附图说明 | ✅ |
| AI | `/api/ai/search-terminology` AI 检索术语库 | ✅ |
| AI | `/api/ai/search-patents` 专利检索 | ✅ |
| 业务 | Cases / Disclosures / Writings / Reviews CRUD | ✅ |
| 业务 | 术语库 CRUD（`/api/terminology`） | ✅ |
| 协同 | 版本快照存储（`sys_writing_version`） | ✅ |
| 数据库 | 8 张表（含 `sys_terminology`）+ AI 日志 | ✅ |
| SQL | `03_terminology.sql` 4 个领域 18 条示例术语 | ✅ |
| 认证 | 登录 / 验证 | ✅ |

---

## 后端待做

### 高优先

*（全部完成）*

### 中优先

| # | 任务 | 说明 |
|---|------|------|
| 2 | **协同编辑 WebSocket/SSE** | 需求要求"30 人同时在线编辑"。当前仅有版本快照存储，无实时同步。需引入 WebSocket |
| 3 | **OCR/图片识别管线** | Qwen 服务有 `analyzeImage`，需对接上传 → OCR → 填入交底书的完整管线 |
| 4 | **附图生成** | 需求要求"主要图的生成"。需对接图片生成模型或至少生成描述 |

### 低优先

| # | 任务 | 说明 |
|---|------|------|
| 5 | ~~数据库 ID 自增~~ | ✅ 已修复，6 个表 `id INT → INT AUTO_INCREMENT` |
| 6 | **上传文件与 AI 管线打通** | 文件上传后仅存磁盘，未与 AI 解析流程关联 |
| 7 | **AI 换成本地免费模型** | 需求硬性要求"不能用付费接口"。当前用的是阿里云 DashScope（Qwen），后续替换为 Ollama / llama.cpp 等 |

---

## 需告知前端的清单（我不负责）

> 以下问题后端 API 已就绪，但前端未对接，需前端同学处理。

| 前端组件 | 问题 |
|----------|------|
| `m06/ai-inspection.tsx` | 专利检索结果硬编码，`setTimeout(3000)` 假检索 |
| `m06/disclosure-supplement.tsx` | 交底书补全硬编码 |
| `m06/supplement-mode-selection.tsx` | 补全模式选择硬编码 |
| `m07/dual-doc-workspace.tsx` | 交底书段落硬编码，未调 `/api/disclosures` |
| `m07/onlyoffice-editor.tsx` | AI 辅助 `setTimeout(1500)` 假生成 |
| `m07/five-books-page.tsx` | 五书生成 `setTimeout(800)` 逐个假生成 |
| `m07/claims-writing-page.tsx` | 权利要求硬编码 |
| `m07/full-review-page.tsx` | AI 审查结果硬编码 |
| `m08/review-decision.tsx` | 审核问题硬编码 |

**一句话**：所有前端 AI 相关组件都是硬编码假数据，需全部对接到后端 API。

---

## 后端修复顺序建议

1. 协同编辑 WebSocket
3. 附图/OCR
4. 数据库 ID 自增修复 + 上传管线打通
5. AI 换本地模型（最后再做）
